{
  "name": "@qr-suite/terminal",
  "licence": "MIT",
  "version": "0.0.1",
  "main": "dist/main",
  "types": "dist/main",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf ./dist ./tsconfig.build.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json --verbose"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@qr-suite/matrix": "^0.0.1",
    "chalk": "^4.1.0"
  }
}
