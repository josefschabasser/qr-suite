{
  "name": "@qr-suite/terminal-cli",
  "licence": "MIT",
  "version": "0.0.1",
  "files": [
    "dist"
  ],
  "bin": {
    "qrsuite-terminal": "dist/main.js"
  },
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf ./dist ./tsconfig.build.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json --verbose"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "@types/yargs": "^16.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@qr-suite/terminal": "^0.0.1",
    "chalk": "^4.1.1",
    "yargs": "^16.2.0"
  }
}
